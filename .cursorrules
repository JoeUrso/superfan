When you need a component, do this:
- think carefully about the component
- generate a prompt for the component
- then with the prompt, generate a clickable link:
[component name](v0.dev/chat?q={prompt})
- make sure prompt is url encoded